.DS_Store
node_modules/
dist/
coverage/
# WorkBuddy workspace metadata（identity / memory / 跨项目笔记）
.workbuddy/
# 本地 issue tracker（per AGENTS.md："Issues 以本地 Markdown 文件形式追踪于 .scratch/<feature>/"）
.scratch/

# workspace 构建产物（发布时由 npm run build 生成）
packages/*/dist

# workspace 构建产物（发布时由 npm run build 生成）
packages/*/dist
# tsup 运行时临时 bundle
packages/*/tsup.config.bundled_*.mjs
