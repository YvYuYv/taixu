# WebSocket 挂起断连：框架重连连接，应用重建订阅

ADR-0013 定了挂起即 close(1000)。"谁负责重连"若为纯应用责任，忘记监听恢复事件的应用会静默失联。决策：**框架在挂起时记录所有 WS 连接描述符（URL、协议、应用归属），恢复时自动重建连接；但订阅状态由应用自行重建**——框架无法知道应用的频道订阅语义。备选"沙箱包装 WS 构造器伪装成同一连接"被否：它制造状态机撒谎者——应用以为连接一直活着，断层期消息静默蒸发，违反"丢失必须显式"的总原则。
