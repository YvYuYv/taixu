# 服务替换触发的 Fiber 重跑视为整应用重挂载

Cordis 的响应式 coeffect 会在被注入服务被替换时**重跑** Fiber 的 apply——对微应用而言这意味着旧执行环境的全部 effect 回滚后重新执行，语义等价于整应用重挂载（HMR 全量重启语义），而非原位热替换。决策：接受该语义为框架行为；适配器的 unmount 清理器必须校验容器已清空（防止重跑时旧 DOM 残留导致双挂载）。备选"冻结依赖禁止运行时替换服务"被否：这会牺牲 Cordis 的核心能力，且框架自身的热更新与降级恢复依赖服务可替换性。
