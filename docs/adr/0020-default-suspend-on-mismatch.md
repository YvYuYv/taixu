# 路由失配默认挂起，应用可声明 keepAlive:false

槽位失配后应用进挂起还是直接 dispose，决定保活的默认代价。决策：**默认挂起**（进 ADR-0019 的 LRU 池），应用可显式声明 `keepAlive:false` 直接 dispose。理由：微前端典型交互是页签来回切换，默认 dispose 让每次切换付全量冷启动；LRU 上限已把最坏内存代价钉死，默认挂起的风险有界。备选"按路由声明策略"被否：路由表作者往往不是应用作者，不知道应用的启动代价。
