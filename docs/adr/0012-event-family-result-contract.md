# 调度结果契约按事件族声明，可机器校验

框架内 `serial`/`bail` 调度存在两类结果约定：导航守卫的枚举结果（ADR-0002）与请求-应答的业务载荷。决策：在 cordis-alignment.md 中按事件族（事件名前缀）声明结果契约——`router/guard:*` 族返回守卫枚举，其余请求-应答族返回包络（ADR-0014）或 undefined。不追求全框架统一返回形状（请求-应答载荷形状多样，强塞进枚举是伪统一），也不依赖纯纪律（族边界可写 lint 规则机器校验）。
