# 挂起应用的消息排队待恢复后回放

保活挂起只冻结定时器/rAF，Cordis 事件照常派发到挂起应用的监听器（effect 并未回收）——若直接投递，应用会在"冻结"状态下处理消息，产生与 UI 脱节的状态变更。决策：挂起期间总线为应用排队消息，恢复时按序回放；另设控制消息白名单（如 `app/resume`、`app/dispose`）不经排队直达，否则无法唤醒。备选"挂起即退订"被否：恢复时需重建全部订阅，且丢失挂起期间的消息。
