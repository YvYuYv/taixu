# 快照带版本号 + 可选 migrate 函数，无迁移则丢弃冷启动

ADR-0029 的快照是应用版本 A 的状态，驱逐后应用发版到 B，注水旧结构可能解析崩溃。决策：快照载荷 `{version, data}`；重挂载时版本不匹配则查应用清单上的 `migrate(snapshot, fromVersion)` 函数（可选）——有则迁移后注水，无则丢弃快照冷启动并 `monitor.capture` 上报"快照版本漂移丢弃"。`migrate` 必须是**纯函数、无副作用**（在沙箱外执行，拿不到 ctx）。备选"纯丢弃"被否：频繁发版场景让快照形同虚设。
