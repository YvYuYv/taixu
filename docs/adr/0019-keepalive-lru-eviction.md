# 保活池 LRU 上限 5，超限驱逐为 dispose

挂起应用仍占内存（DOM 冻结保留、JS 堆保留），移动端无上限即 OOM。决策：保活池默认上限 5，LRU 驱逐——超限自动 **dispose** 最久未用的挂起应用（不是另一种挂起），并触发 `app/evicted` 事件进事件契约。备选"不设上限靠系统信号批量卸载"被否：把 OOM 风险留给设备。备选"应用声明优先级"被否：所有作者都会拉满优先级（公地悲剧），等于没有。
