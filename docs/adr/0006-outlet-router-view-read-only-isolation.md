# 按槽位的路由视图隔离仅隔离读侧

每个槽位用 `ctx.isolate('router')` 获得独立 router 视图实例，但仅用于**读取**（本槽位的路径、参数）。写操作（跳转）必须汇聚到全局 NavigationController 做多槽位合并后再写地址栏——若隔离实例各自写 URL，多槽位导航会互相覆盖。备选"读侧也走全局服务 + 槽位参数"被否：隔离读侧让应用代码无需感知槽位，符合 Cordis 注入模型；代价是写侧必须约定唯一的全局入口。
