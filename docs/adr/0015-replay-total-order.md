# 恢复回放期间新消息入队尾，保持全序

ADR-0008 定了按帧分批回放，但回放窗口内新到的实时消息面临两难：立即投递会造成"先见新状态、再见旧状态"的时间倒流，插到队尾则引入延迟。决策：**一律入队尾，保持全序**——1000 条上限 ÷ 每帧 50 条 = 最坏 20 帧（≈333ms）的追平延迟，有界可接受，且与 ADR-0008 的"恢复后主动重拉"契约叠加后风险收敛。备选"新消息立即投递并标 live:true"被否：时间倒流对状态合并逻辑是灾难。备选"打包成 bus/backlog 批量消息"被否：把框架该担的追平复杂度转嫁给每个应用作者，且给包络契约开特例。
