# 权限裁决不做跨调用缓存，规则编译产物挂版本号缓存

scopedFetch 高频裁决需要缓存，但缓存与权限热更新（管理员实时改权限）矛盾。决策：**不做跨调用缓存**——权限表在内存（Map），单次查询是 O(规则数) 匹配，微秒级开销不是瓶颈（真正瓶颈是网络 I/O 本身）；规则匹配的编译产物（glob→RegExp）可缓存，缓存键挂权限表版本号，热更新时版本号 bump 自动失效。备选 TTL 缓存被否：TTL 窗口期是安全漏洞（封禁后 30s 内仍可用）。
