# 内存水位：30s 低频轮询兜底 + 操作触发检查为主

ADR-0026 的 `performance.memory` 需轮询（无事件），但读取 `usedJSHeapSize` 有开销。决策：**低频轮询 30s 一次兜底** + **操作触发检查为主**（每次挂起/恢复/挂载/驱逐操作时顺带检查水位）——被动检查覆盖大部分场景（内存压力通常在挂载新应用时暴露），轮询只是兜底。备选 1s 高频轮询被否：低频场景浪费，高频场景（连续挂载）又不如操作触发及时。
