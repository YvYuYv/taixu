# 槽位（路由概念）与容器（DOM 概念）分层

CONTEXT.md 的"槽位（Outlet）"与异构加载文档的"容器（container）"在文档中混用。决策：明确分层——**槽位是路由/逻辑概念**（URL ↔ 命名挂载点的映射，由 router 解析），**容器是 DOM/物理概念**（槽位物化后的实际 DOM 节点，由 `lifecycle.createOutletContainer()` 创建）。一个槽位任意时刻至多对应一个容器；挂起时容器被摘除到文档片段缓存，但槽位仍在（匹配状态不变）。两词不可互换：路由文档只说槽位，加载/样式文档只说容器。
