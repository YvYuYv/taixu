# AGENTS.md

## Agent skills

### Issue tracker

Issues 以本地 Markdown 文件形式追踪于 `.scratch/<feature>/`。见 `docs/agents/issue-tracker.md`。

### Triage labels

使用默认五角色标签（needs-triage / needs-info / ready-for-agent / ready-for-human / wontfix）。见 `docs/agents/triage-labels.md`。

### Domain docs

单上下文：根级 `CONTEXT.md`（术语表）+ `docs/adr/`（ADR-0001~0060）。见 `docs/agents/domain.md`。
