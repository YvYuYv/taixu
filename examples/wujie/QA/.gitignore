# 采集产物（可由 run-suite.mjs / probe-font.mjs 重新生成，不进版本控制）
node_modules/
shots/
shots-compare/
suite.*.json
font-probe.json

# 早期的一次性快照（命名如 baseline.official-vue.json）。
# 注意：**回归基线在 baseline/ 目录，是进版本控制的**（CI 门禁要用）。
baseline.*.json
